import styles from "./NotFoundPage.module.css";

const NotFoundPage = () => {
  return (
    <section>
      <h1>Ops! The page you requested was not found</h1>
    </section>
  );
};

export default NotFoundPage;
